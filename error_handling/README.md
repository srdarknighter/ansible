Error handling in Ansible when a task fails it stops executing on that particular vm and stops execution of tasks in that but does it in other vm's.
Put the ansible vault password in aws secret management or a similar service
Put diff passwords for for the different roles/playbook variables vaults